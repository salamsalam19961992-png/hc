# نظام إدارة وصولات توزيع الصحة

نظام متكامل لإدارة وتوزيع الوصولات الطبية على أقسام الصحة المختلفة مع واجهة جميلة ومتجاوبة.

## المميزات

### 🏥 إدارة العيادات
- إضافة وتعديل وحذف العيادات
- تصنيف العيادات (داخلية/خارجية)
- إدارة مواقع العيادات

### 💰 الفئات المثمنة
- فئة 1500 دينار (100 وحدة)
- فئة 3000 دينار (100 وحدة)
- فئة 4000 دينار (100 وحدة)
- فئة 5000 دينار (100 وحدة)
- فئة 25000 دينار (50 وحدة)
- فئة 40000 دينار (50 وحدة)

### 📄 الفئات غير المثمنة
- وصل الحاسبة 92 (50 وحدة)
- وصل الحاسبة 37 (50 وحدة)
- وصل قبض تامينات الجناح الخاص (50 وحدة)
- قائمة حساب الجناح الخاص (50 وحدة)
- تقارير امراض مزمنة (50 وحدة)
- بطاقة امرض مزمنة (50 وحدة)
- مستند اخراج تخزين للبطاقات (50 وحدة)
- مستند اخراج تخزين للادوية (50 وحدة)
- مستند ادخال تخزين (50 وحدة)
- قرار لجنة طبية (50 وحدة)

### 📊 التقارير والإحصائيات
- إجمالي الفئات المثمنة
- إجمالي الفئات غير المثمنة
- توزيع العيادات
- تقارير مفصلة للتوزيع

## التقنيات المستخدمة

- **HTML5** - هيكل الصفحة
- **CSS3** - التصميم والأنماط
- **JavaScript ES6+** - الوظائف والتفاعل
- **Firebase Firestore** - قاعدة البيانات
- **Font Awesome** - الأيقونات
- **Google Fonts** - الخطوط العربية

## التثبيت والإعداد

### 1. إعداد Firebase

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. أنشئ مشروع جديد
3. فعّل Firestore Database
4. انسخ إعدادات المشروع

### 2. تحديث إعدادات Firebase

افتح ملف `firebase-config.js` وأضف إعدادات مشروعك:

```javascript
const firebaseConfig = {
    apiKey: "your-api-key",
    authDomain: "your-project.firebaseapp.com",
    projectId: "your-project-id",
    storageBucket: "your-project.appspot.com",
    messagingSenderId: "123456789",
    appId: "your-app-id"
};
```

### 3. إعداد قواعد Firestore

أضف هذه القواعد في Firebase Console > Firestore Database > Rules:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

### 4. تشغيل النظام

1. افتح ملف `index.html` في المتصفح
2. سيتم تحميل البيانات الافتراضية تلقائياً
3. ابدأ بإضافة العيادات والفئات

## الاستخدام

### إضافة عيادة جديدة
1. انتقل إلى تبويب "إدارة العيادات"
2. اضغط على "إضافة عيادة جديدة"
3. أدخل اسم العيادة ونوعها وموقعها

### إضافة فئة جديدة
1. انتقل إلى تبويب "الفئات المثمنة" أو "الفئات غير المثمنة"
2. اضغط على "إضافة فئة جديدة"
3. أدخل تفاصيل الفئة

### توزيع الوصولات
1. اضغط على "إضافة وصول جديد"
2. اختر العيادة والفئة
3. أدخل الكمية والملاحظات
4. احفظ الوصول

### عرض التقارير
1. اضغط على "التقارير"
2. شاهد الإحصائيات والتوزيعات

## هيكل قاعدة البيانات

### المجموعات (Collections)

#### pricedCategories
```javascript
{
  id: "string",
  name: "string",
  price: "number",
  quantity: "number",
  total: "number"
}
```

#### unpricedCategories
```javascript
{
  id: "string",
  name: "string",
  quantity: "number"
}
```

#### clinics
```javascript
{
  id: "string",
  name: "string",
  type: "string",
  location: "string"
}
```

#### entries
```javascript
{
  id: "string",
  clinicId: "string",
  categoryId: "string",
  quantity: "number",
  notes: "string",
  timestamp: "timestamp",
  type: "string"
}
```

## المميزات التقنية

- **تصميم متجاوب** - يعمل على جميع الأجهزة
- **واجهة عربية** - دعم كامل للغة العربية
- **أداء عالي** - تحميل سريع وتفاعل سلس
- **أمان** - حماية البيانات مع Firebase
- **سهولة الاستخدام** - واجهة بديهية وبسيطة

## الدعم والمساعدة

لأي استفسارات أو مشاكل تقنية، يرجى التواصل مع فريق التطوير.

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام التجاري والشخصي.
