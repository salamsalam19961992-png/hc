# دليل الإعداد السريع

## خطوات الإعداد

### 1. إعداد Firebase
1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اضغط على "إضافة مشروع"
3. أدخل اسم المشروع (مثل: health-access-system)
4. فعّل Google Analytics (اختياري)
5. اضغط "إنشاء مشروع"

### 2. تفعيل Firestore
1. في لوحة Firebase، اضغط على "Firestore Database"
2. اضغط "إنشاء قاعدة بيانات"
3. اختر "بدء في وضع الاختبار"
4. اختر موقع الخادم (يفضل الأقرب لمنطقتك)

### 3. الحصول على إعدادات المشروع
1. اضغط على "إعدادات المشروع" (⚙️)
2. انتقل إلى تبويب "عام"
3. في قسم "تطبيقاتك"، اضغط "إضافة تطبيق"
4. اختر "ويب" (🌐)
5. أدخل اسم التطبيق
6. انسخ إعدادات Firebase

### 4. تحديث الملفات
افتح ملف `script.js` واستبدل إعدادات Firebase:

```javascript
const firebaseConfig = {
    apiKey: "AIzaSy...", // من Firebase Console
    authDomain: "your-project.firebaseapp.com",
    projectId: "your-project-id",
    storageBucket: "your-project.appspot.com",
    messagingSenderId: "123456789",
    appId: "1:123456789:web:abcdef..."
};
```

### 5. إعداد قواعد الأمان
1. في Firestore Database، اضغط على "قواعد"
2. استبدل القواعد الحالية بهذه القواعد:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

3. اضغط "نشر"

### 6. تشغيل النظام
1. افتح ملف `index.html` في المتصفح
2. سيتم تحميل البيانات الافتراضية تلقائياً
3. ابدأ بإضافة العيادات والفئات

## البيانات الافتراضية

سيتم تحميل البيانات التالية تلقائياً:

### الفئات المثمنة:
- فئة 1500 ريال (100 وحدة)
- فئة 3000 ريال (100 وحدة)
- فئة 4000 ريال (100 وحدة)
- فئة 5000 ريال (100 وحدة)
- فئة 25000 ريال (50 وحدة)
- فئة 40000 ريال (50 وحدة)

### الفئات غير المثمنة:
- وصل الحاسبة 92 (50 وحدة)
- وصل الحاسبة 37 (50 وحدة)
- وصل قبض تامينات الجناح الخاص (50 وحدة)
- قائمة حساب الجناح الخاص (50 وحدة)
- تقارير امراض مزمنة (50 وحدة)
- بطاقة امرض مزمنة (50 وحدة)
- مستند اخراج تخزين للبطاقات (50 وحدة)
- مستند اخراج تخزين للادوية (50 وحدة)
- مستند ادخال تخزين (50 وحدة)
- قرار لجنة طبية (50 وحدة)

## استكشاف الأخطاء

### مشكلة: "Firebase not initialized"
**الحل:** تأكد من تحديث إعدادات Firebase في ملف `script.js`

### مشكلة: "Permission denied"
**الحل:** تأكد من إعداد قواعد Firestore بشكل صحيح

### مشكلة: البيانات لا تظهر
**الحل:** 
1. تأكد من اتصال الإنترنت
2. تحقق من إعدادات Firebase
3. افتح Developer Tools (F12) وتحقق من الأخطاء

### مشكلة: الواجهة لا تعمل
**الحل:**
1. تأكد من تحميل جميع الملفات
2. تحقق من مسارات الملفات
3. تأكد من دعم JavaScript في المتصفح

## الدعم

للمساعدة التقنية، تأكد من:
1. استخدام متصفح حديث (Chrome, Firefox, Safari, Edge)
2. تفعيل JavaScript
3. اتصال مستقر بالإنترنت
4. إعدادات Firebase صحيحة
